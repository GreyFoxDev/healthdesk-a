defmodule Data.Commands.Disposition do
  # @moduledoc false

  # use Data.Commands, schema: Disposition

  # def all(team_id),
  #   do: Command.execute_task_with_results(fn -> Read.all(team_id) end)
end
